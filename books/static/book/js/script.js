// Main script.js - Uses shared utilities to avoid redundancy
console.log("Ebook Library Manager: script.js loaded");

// Page-specific functionality that doesn't belong in shared utilities
document.addEventListener('DOMContentLoaded', function () {
    // Shared utilities are auto-initialized in shared-utils.js
    
    // Any page-specific code can go here
    console.log('Page-specific initialization complete');
});